---
classes:
  - packer::updates
  - packer::clock

puppetmaster: 'puppet'

python::virtualenv: 'present'
python::pip: 'present'
python::dev: 'present'
apache::default_vhost: false
apache::purge_vhost_dir: true
